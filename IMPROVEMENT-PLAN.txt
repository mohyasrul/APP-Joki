================================================================================
  JOKSKUY — IMPROVEMENT PLAN (Audit 2 Maret 2026)
================================================================================

STATUS LEGEND:
  [ ] Belum dikerjakan
  [x] Selesai
  [-] Dibatalkan / Tidak jadi

================================================================================
  P0 — CRITICAL (Harus segera diperbaiki)
================================================================================

  [x] #1  get_dashboard_stats() bocor ke client
          Semua user (termasuk client) bisa panggil RPC ini dan lihat total
          income + semua recent orders. 
          Solusi: tambah admin role check di function body.
          File: upgrade-functions.sql
          DONE: commit 8931b9a — admin role check added

  [x] #2  Tidak ada Error Boundary
          Error React yang tidak ter-handle menyebabkan white screen.
          Solusi: buat ErrorBoundary component + fallback UI + tombol retry.
          File: App.jsx (baru: ErrorBoundary.jsx)
          DONE: commit 8931b9a — ErrorBoundary wraps entire app

  [x] #41 HOTFIX: orders_client_id_fkey violates pada akun baru
          Akun yang registrasi SETELAH email verification diaktifkan gagal
          membuat order karena row di public.profiles tidak pernah dibuat.
          Root cause: saat email verification ON, signUp() mengembalikan
          data.session = null, sehingga insert profiles diblokir RLS
          (auth.uid() = null) dan gagal secara diam-diam.
          Solusi:
            1. DB trigger handle_new_user() — SECURITY DEFINER, insert profile
               saat row baru ditambahkan ke auth.users (sebelum email diverif).
            2. Metadata full_name & phone dikirim via options.data di signUp()
               agar trigger bisa baca tanpa session.
            3. Guard manual insert di signUp(): hanya jalan jika data.session
               ada (auto-confirm mode), tidak lagi silent-fail.
            4. Safety-net di fetchProfile(): jika PGRST116 (no row), auto-insert
               profile — menangani akun lama yang sudah terlanjur tanpa profile.
            5. Back-fill query di upgrade-v8.sql untuk akun confirmed yang
               profilnya hilang sebelum fix ini di-deploy.
          File: upgrade-v8.sql (baru), AuthContext.jsx
          DONE: upgrade-v8.sql + AuthContext.jsx patched

================================================================================
  P1 — HIGH (Sangat penting)
================================================================================

  [x] #3  KelolaLayanan & AdminSettings pakai custom modal
          Dua halaman ini masih buat modal <div> sendiri, bukan pakai
          Modal.jsx yang sudah accessible (focus trap, Escape, aria-modal).
          File: KelolaLayanan.jsx, AdminSettings.jsx
          DONE: commit e1825fd — refactored to Modal.jsx

  [x] #4  Toast tidak diumumkan ke screen reader
          Toast container tidak punya role="alert" atau aria-live="polite".
          File: Toast.jsx
          DONE: commit e1825fd — added role/aria-live attributes

  [x] #5  Tidak ada email verification
          Setelah register, user langsung masuk tanpa verifikasi email.
          Solusi: tampilkan halaman "cek email", enforce email_confirmed_at.
          File: AuthContext.jsx, Register.jsx
          DONE: commit e1825fd — emailRedirectTo + confirmation screen

  [x] #6  Keuangan fetch semua orders client-side
          Tidak ada server-side aggregation atau date filter. Bottleneck
          saat data banyak.
          Solusi: buat RPC get_keuangan_stats(p_year INT).
          File: Keuangan.jsx, upgrade-v6.sql
          DONE: commit 6c48286 — get_keuangan_summary() RPC + paginated transactions

  [x] #7  AdminOrders search download semua data
          Search dengan join fetches all rows lalu filter client-side.
          Solusi: gunakan Supabase textSearch atau RPC dengan ILIKE.
          File: AdminOrders.jsx, upgrade-v6.sql
          DONE: commit 6c48286 — search_orders() RPC with server-side ILIKE

  [x] #8  Tidak ada notifikasi ke admin saat bukti transfer diupload
          Admin hanya dapat push notification untuk order baru dan custom
          request, tapi TIDAK untuk "bukti bayar uploaded — perlu verifikasi".
          Solusi: tambah DB trigger.
          File: upgrade-v6.sql
          DONE: commit 6c48286 — notify_admins_payment_uploaded() trigger

================================================================================
  P2 — MEDIUM (Penting untuk UX & kualitas)
================================================================================

  [x] #9  Validasi phone number absent
          Register menerima string apapun sebagai nomor HP.
          Solusi: regex validate ^(08|+62)[0-9]{8,13}$.
          File: Register.jsx

  [x] #10 Password show/hide toggle
          Login & Register tidak ada tombol mata untuk lihat password.
          File: Login.jsx, Register.jsx

  [x] #11 Toast overlap BottomNav
          Toast container di bottom-4 right-4 tertutup BottomNav di mobile.
          Solusi: bottom-20 saat ada BottomNav.
          File: Toast.jsx

  [x] #12 OG Image masih vite.svg
          Open Graph image generic, bukan branded.
          File: index.html

  [x] #13 maximum-scale=1 blokir user zoom
          Accessibility violation — user yang perlu zoom tidak bisa.
          Solusi: hapus maximum-scale=1 dari viewport meta.
          File: index.html

  [x] #14 Promo tidak ada tanggal expired
          Tabel promos tidak punya valid_until/expired_at. Promo aktif
          selamanya.
          Solusi: tambah kolom + check di claim_promo().
          File: upgrade-v2.sql (baru migration)

  [x] #15 Promo persen tanpa max cap
          Promo 100% bisa bikin harga Rp 0 tanpa batas.
          Solusi: tambah kolom max_potongan.
          File: upgrade-v2.sql (baru migration)

  [x] #16 Duplikasi kode: formatSize, getFileIcon, STATUS_COLORS
          Fungsi dan constants yang sama di 2-3 file berbeda.
          Solusi: pindahkan ke src/lib/utils.js dan src/lib/constants.js.
          File: OrderDetail.jsx, AdminOrders.jsx, PesananSaya.jsx, Dashboard.jsx

  [x] #17 Tidak ada fitur change password
          Halaman Profile hanya edit nama, HP, avatar.
          File: Profile.jsx

  [x] #18 NotificationBell dropdown tidak lock scroll
          Di mobile, user bisa scroll page di belakang dropdown notifikasi.
          File: NotificationBell.jsx

  [x] #19 Google Fonts blocking render
          Font Inter loaded via blocking <link>.
          Solusi: tambah font-display: swap atau <link rel="preload">.
          File: index.html

  [x] #20 Tidak ada .env.example
          Developer baru tidak tahu env var apa saja yang perlu diset.
          File: (baru) .env.example

  [-] #21 Tidak ada offline fallback page
          PWA offline menampilkan error browser generic.
          File: sw.js, (baru) offline.html

================================================================================
  P3 — LOW (Nice to have / Polish)
================================================================================

  [x] #22 Tidak ada CSP header
          Tidak ada Content-Security-Policy di security headers Vercel.
          File: vercel.json
          DONE: CSP header ditambahkan ke vercel.json

  [x] #23 Rating tanpa CHECK constraint
          orders.rating bisa diisi 0 atau 999.
          Solusi: ALTER TABLE orders ADD CONSTRAINT rating_check
          CHECK (rating >= 1 AND rating <= 5).
          DONE: upgrade-v9.sql — rating_range_check constraint (IS NULL OR 1-5)

  [x] #24 Notification cleanup
          Tabel notifications tumbuh tanpa batas.
          Solusi: scheduled cleanup atau TTL (delete > 30 hari).
          DONE: upgrade-v9.sql — cleanup_old_notifications() + idx_notifications_created_at
                (pg_cron schedule tersedia, lihat komentar di upgrade-v9.sql)

  [x] #25 Image loading="lazy" missing
          Avatar dan bukti transfer image tidak lazy loaded.
          File: Navbar.jsx, OrderDetail.jsx
          DONE: loading="lazy" ditambahkan ke semua img tag terkait

  [x] #26 BottomNav missing aria-current="page"
          Active tab hanya ditandai warna, tidak ada ARIA attribute.
          File: BottomNav.jsx
          DONE: aria-current={isActive ? 'page' : undefined} pada setiap NavLink

  [x] #27 Loading spinner tanpa aria-label
          Spinner loading tidak punya role="status" atau label.
          File: App.jsx, ProtectedRoute.jsx
          DONE: role="status" + aria-label ditambahkan ke semua spinner wrapper

  [x] #28 Filter buttons tanpa aria-pressed
          Tombol filter di Katalog dan PesananSaya tidak ada state ARIA.
          File: Katalog.jsx, PesananSaya.jsx
          DONE: aria-pressed={aktif} ditambahkan ke semua filter button

  [x] #29 timeAgo di NotificationBell
          Harusnya dipindahkan ke utils.js untuk reuse.
          File: NotificationBell.jsx, utils.js
          DONE: timeAgo dipindah ke utils.js, dihapus dari constants.js, import diupdate

  [x] #30 Tidak ada robots.txt dan sitemap.xml
          Untuk SEO basic.
          File: (baru) public/robots.txt, public/sitemap.xml
          DONE: kedua file dibuat di public/

  [x] #31 No Twitter Card meta tags
          Hanya OG tags, tidak ada twitter:card dll.
          File: index.html
          DONE: sudah ada sebelumnya (twitter:card, title, description, image)

  [x] #32 Icon PWA hanya 2 ukuran
          Hanya 192x192 dan 512x512, kurang untuk full device coverage.
          File: vite.config.js, public/icons/
          DONE: purpose: 'any' ditambahkan ke icon 192 dan 512; maskable tetap ada

================================================================================
  P4 — FITUR BARU (Enhancement)
================================================================================

  [x] #33 Chat/Messaging admin-client
          Tidak ada cara komunikasi dalam app antara client dan admin
          tentang order.
          Solusi: chat sederhana per-order.
          DONE: upgrade-v10.sql (order_messages table + RLS + Realtime),
                OrderChat.jsx component, OrderDetail.jsx (client view),
                AdminOrders.jsx (admin modal + MessageCircle button per order)
          Impact: High

  [x] #34 Order timeline/activity log
          Tidak ada history perubahan status order.
          Solusi: tabel order_activities dengan trigger.
          DONE: upgrade-v10.sql (order_activities table + trigger),
                OrderTimeline.jsx component, OrderDetail.jsx
          Impact: Medium

  [x] #35 File attachment saat buat order
          Client tidak bisa upload materi pendukung saat submit order.
          DONE: upgrade-v10.sql (order_files jsonb column),
                BuatOrder.jsx (file picker + upload), OrderDetail.jsx (display)
          Impact: Medium

  [x] #36 Export CSV di AdminOrders
          Admin hanya bisa export di Keuangan, perlu juga di orders.
          DONE: AdminOrders.jsx (exportCSV function + FileDown button)
          Impact: Low

  [x] #37 Sort & search di PesananSaya
          Client tidak bisa search atau sort pesanan mereka.
          DONE: PesananSaya.jsx (search input + sort select)
          Impact: Low

  [x] #38 Notification preferences
          User tidak bisa pilih jenis notifikasi yang mau diterima.
          DONE: upgrade-v11.sql (preferences jsonb column),
                Profile.jsx (toggle switches)
          Impact: Low

  [x] #39 Account deletion
          Tidak ada fitur hapus akun (GDPR-like compliance).
          DONE: upgrade-v11.sql (delete_own_account() RPC),
                Profile.jsx (Zona Bahaya + confirm modal)
          Impact: Low

  [x] #40 Dark/Light mode toggle
          Saat ini fixed dark mode.
          DONE: useTheme.js hook, index.css (data-theme light overrides),
                Navbar.jsx (Sun/Moon toggle button)
          Impact: Low

================================================================================
  P5 — MAXIMIZE EXISTING FEATURES
================================================================================

  P5 Batch A — Data Model & SQL Foundation
  ----------------------------------------
  Semua perubahan DB dikerjakan dulu agar Batch B/C/D bisa langsung UI.
  File: upgrade-v13.sql (baru)

  [ ] #42-DB  layanan.estimasi_hari integer
              Estimasi hari pengerjaan per layanan.

  [ ] #43-DB  orders.catatan_internal text
              Note admin, tidak visible ke client.
              RLS: SELECT hanya untuk role='admin'.

  [ ] #44-DB  custom_requests.budget_min / budget_max integer
              Range estimasi budget dari client.

  [ ] #45-DB  custom_requests.lampiran_files jsonb DEFAULT '[]'
              File attachment di custom request.

  [ ] #46-DB  settings row 'app_config'
              INSERT IF NOT EXISTS — untuk default_max_revisi + announcement.

  [ ] #47-DB  RPC get_client_stats(p_user_id uuid)
              Return { total_orders, selesai, pengeluaran_total, bergabung_sejak }
              Dipakai di Profile client.

  P5 Batch B — Admin Power Features
  ----------------------------------
  Semua perbaikan sisi admin yang memperkuat operasional sehari-hari.

  [ ] #42 KelolaLayanan improvements
          - Tambah field estimasi_hari di form tambah/edit
          - Tombol duplikat layanan (copy + " (copy)" di judul)
          File: KelolaLayanan.jsx

  [ ] #43 AdminOrders improvements
          - Urgency indicator: tenggat_waktu ≤ 2 hari → border kuning,
            sudah lewat → border merah + badge "TERLAMBAT"
          - Tampilkan order_files client di dalam row/modal
          - Field Catatan Internal di upload hasil modal
          - Filter date range (dari–sampai) di filter bar
          File: AdminOrders.jsx

  [ ] #44 AdminRequests improvements
          - Search input + filter tab status (Semua/Menunggu/Diterima/Ditolak)
          - Tampilkan budget range dari client jika diisi
          File: AdminRequests.jsx

  [ ] #45 Keuangan improvements
          - Year selector dropdown → pass p_year ke RPC get_keuangan_summary
          - Section "Belum Dibayar": list + total nilai outstanding
          File: Keuangan.jsx

  [ ] #46 Dashboard improvements
          - Stat card ke-5: Pending Custom Requests
          - Rata-rata rating dari orders
          - Recent orders clickable → navigate ke /admin/orders
          File: Dashboard.jsx

  [ ] #47 Settings — Pengaturan Umum
          - Default max revisi (integer)
          - Teks pengumuman/banner (nullable, tampil di Katalog client)
          File: AdminSettings.jsx

  P5 Batch C — Client Flow Improvements
  --------------------------------------
  Perbaikan alur utama: browse → order → bayar → terima hasil.

  [ ] #48 Katalog improvements
          - Sort layanan: Terbaru / Harga ↑ / Harga ↓
          - Tampilkan estimasi_hari di card ("⏱ ~3 hari")
          - Banner pengumuman dari settings app_config (dismiss via sessionStorage)
          File: Katalog.jsx

  [ ] #49 CustomRequest improvements
          - Field budget range (min–max, opsional)
          - File attachment max 3 file 5MB → order-attachments/requests/{user_id}/
          File: CustomRequest.jsx

  [ ] #50 OrderDetail improvements
          - Countdown deadline widget (hijau/kuning/merah berdasarkan sisa waktu)
          - Tombol copy Order ID ke clipboard
          - Tombol "Download Semua" jika hasil_files.length > 1
          File: OrderDetail.jsx

  P5 Batch D — Statistics & Profile Polish
  -----------------------------------------
  Fitur ringkasan informasi untuk client, paling ringan.

  [ ] #51 PesananSaya improvements
          - Stats bar: Aktif | Selesai | Total Pengeluaran
          - Tambah filter tab "Belum Bayar"
          File: PesananSaya.jsx

  [ ] #52 Profile — Statistik Akun
          - Card: bergabung sejak, total order, total pengeluaran
          - Pakai RPC get_client_stats()
          File: Profile.jsx

================================================================================
  URUTAN IMPLEMENTASI
================================================================================

  Batch 1 — Security & Stability (P0):  #1, #2          ✓ DONE
  Batch 2 — Core UX Issues (P1):        #3, #4, #5, #6, #7, #8  ✓ DONE
  Batch 3 — Quality & Polish (P2):       #9 - #21        ✓ DONE
  Batch 4 — Clean up (P3):              #22 - #32        ✓ DONE
  Batch 5 — New Features (P4):          #33 - #40        ✓ DONE
  Batch 6 — Maximize Features (P5):     #42 - #52        [ ] IN PLANNING
    P5-A: upgrade-v13.sql (DB foundation)
    P5-B: Admin power features (#42–#47)
    P5-C: Client flow improvements (#48–#50)
    P5-D: Stats & polish (#51–#52)

  SQL migrations to run in Supabase SQL Editor (in order):
    upgrade-v8.sql   — #41 orders_client_id_fkey hotfix
    upgrade-v9.sql   — #23 rating constraint, #24 notif cleanup
    upgrade-v10.sql  — #33 chat, #34 timeline, #35 file attachments
    upgrade-v11.sql  — #38 notif prefs, #39 delete account
    upgrade-v12.sql  — chat notification trigger
    upgrade-v13.sql  — P5 data model (belum dibuat)

================================================================================
  CATATAN
================================================================================

  - Jalankan semua file SQL baru di Supabase SQL Editor setelah commit
  - Setiap batch harus di-test dan di-commit terpisah
  - Update status di file ini setelah setiap item selesai

================================================================================


================================================================================
  UI REDESIGN PLAN — Kombinasi 4 Referensi Desain (4 Maret 2026)
================================================================================

  SUMBER REFERENSI:
    contohui.html   — Light sidebar, Orange brand (Inter font)
                      Elemen adoptable: stat card trend badge, pro banner pattern
    contohui_3.html — Dark sidebar + glassmorphic header (Plus Jakarta Sans)
                      Elemen adoptable: sidebar groups, segmented tab, island
                      bottom nav, shine-sweep animation, status badge border+dot
    contohui_4.html — Top-nav only, lime green, checkbox table (Plus Jakarta Sans)
                      Elemen adoptable: rounded-[2rem] table card, custom
                      checkbox, stat card icon-kanan layout, cmd+K search hint
    index.css (existing) — Token system, .glass, .glow, .gradient-text — TETAP

  PRINSIP:
    - Warna / CSS token TIDAK diubah (indigo/purple dark tetap jadi design language)
    - Hanya shape, layout, dan detail visual yang di-upgrade
    - Sidebar hanya untuk admin; client tetap pakai topnav
    - Semua perubahan backward-compatible (tidak ada breaking change)

================================================================================
  FASE 1 — FOUNDATION (Global)
================================================================================

  [x] UI-1.1  Font: Inter → Plus Jakarta Sans
              File  : index.html (tambah Google Fonts link)
                      src/index.css (update --font-sans)
              Source: contohui_3.html + contohui_4.html
              Effort: Sangat rendah

  [x] UI-1.2  Sidebar Layout (Desktop only — admin only)
              Buat komponen baru: src/components/Sidebar.jsx
              Ubah: src/components/Layout.jsx → tambah md:flex wrapper
              Spec:
                - bg: bg-slate-900/60 backdrop-blur-xl border-r border-white/5
                - Nav digroup dalam sections: "Menu Utama" & "Keuangan & Laporan"
                - Section label: text-slate-500 uppercase tracking-wider text-xs
                - Active item: bg-gradient-to-r from-primary to-purple-500
                - Shine-sweep hover: overflow-hidden + translate-x group-hover trick
                - Badge counter: bg-white/20 text-xs rounded-md di dalam active item
                - User card footer: avatar + nama + role badge
                - Breakpoint: hidden md:flex (tidak tampil di mobile)
              Source: contohui_3.html
              Effort: Tinggi

  [-] UI-1.3  Navbar Client — Tambah cmd+K hint pill (opsional/jika ada search)
              File  : src/components/Navbar.jsx
              Source: contohui_3.html + contohui_4.html
              Effort: Sangat rendah

  [x] UI-1.4  BottomNav — Island Floating Style (Mobile)
              File  : src/components/BottomNav.jsx
              Spec:
                - Container: fixed bottom-6 left-4 right-4 rounded-3xl
                - bg: bg-slate-900/95 backdrop-blur-xl border border-white/10
                - Active item: relative -top-4 + border-[5px] border-[#0f0a2e] + icon gradient
                - PERTAHANKAN: env(safe-area-inset-bottom) + badge merah
              Source: contohui_3.html
              Effort: Sedang

================================================================================
  FASE 2 — KOMPONEN SHARED
================================================================================

  [x] UI-2.1  StatusBadge — Shared Component Baru
              File  : src/components/StatusBadge.jsx (baru)
              DONE  : rounded-full border pill, animated dot for active statuses,
                      covers order/payment/custom-request statuses.
                      Adopted in: AdminOrders, AdminRequests, Dashboard,
                                  PesananSaya (orders + custom requests)

  [x] UI-2.2  Segmented Tab Control (Shared pattern)
              DONE  : .segmented-tab-container + .segmented-tab-item CSS classes added.
                      Adopted in: AdminOrders, AdminRequests, PesananSaya (filters),
                                  Keuangan (year selector).
                      Light mode override added in index.css.

  [x] UI-2.3  Stat Cards — Layout Horizontal + Trend Badge
              DONE  : icon w-11/12 rounded-2xl at RIGHT, value + label at LEFT.
                      Trend badge wired (optional prop).
                      Adopted in: Dashboard (6 cards), PesananSaya (stats bar),
                                  Keuangan (4 summary cards).

================================================================================
  FASE 3 — HALAMAN ADMIN
================================================================================

  [x] UI-3.1  Dashboard.jsx
              DONE  : overflow-hidden + shimmer span on quick action buttons
                      (same group-hover:translate-x-full pattern as Sidebar).

  [x] UI-3.2  AdminOrders.jsx
              DONE  : order item wrapper → glass card-table p-5 (.card-table = border-radius 2rem).
                      Deadline urgent border already in place.

  [x] UI-3.3  AdminRequests.jsx
              DONE  : segmented tabs + StatusBadge already adopted in Fase 2.

  [x] UI-3.4  Keuangan.jsx
              DONE  : year selector → segmented control + horizontal stat cards in Fase 2.

  [x] UI-3.5  AdminSettings.jsx
              DONE  : relative overflow-hidden + shimmer span on menu item buttons
                      (consistent with Sidebar and Dashboard quick actions).

================================================================================
  FASE 4 — HALAMAN CLIENT
================================================================================

  [x] UI-4.1  PesananSaya.jsx
              DONE  : segmented tabs + StatusBadge + horizontal stats in Fase 2.

  [x] UI-4.2  OrderDetail.jsx
              DONE  : StatusBadge for status_pekerjaan + status_pembayaran.
                      Removed STATUS_MAP / BAYAR_MAP dead code.
                      OrderTimeline: animate-ping ring on latest activity (idx=0).

  [x] UI-4.3  Katalog.jsx
              DONE  : ⌘K hint kbd pill in search input (hidden on mobile).
                      Sort → segmented control (3 options).
                      Service card: StatusBadge 'Tersedia' added (with new entry
                      in StatusBadge BADGE_CONFIG).

================================================================================
  FASE 5 — CSS ADDITIONS (index.css)
================================================================================

  [x] UI-5.1  Tambah CSS ke src/index.css (TANPA menghapus yang ada):
              - @font-face / font-family update → Plus Jakarta Sans  ✓ (Fase 1)
              - .sidebar-shine     : shine-sweep animation untuk active nav item ✓ (Fase 1)
              - .floating-nav      : styles island bottom nav (backdrop + rounded-3xl) ✓ (Fase 1)
              - .segmented-tab     : base styles segmented control container  ✓ (Fase 2)
              - .card-table        : rounded-[2rem] wrapper untuk tabel  ✓ (Fase 3)

================================================================================
  URUTAN IMPLEMENTASI (Priority Order)
================================================================================

  Urutan  Item        Deskripsi                          Impact        Effort
  ------  ----------  ---------------------------------  ------------  --------
  1       UI-1.1      Font update                        Semua halaman Sangat rendah
  2       UI-2.1      StatusBadge component              Semua halaman Rendah
  3       UI-1.4      BottomNav island floating          Mobile UX     Sedang
  4       UI-2.2      Segmented tabs                     5 halaman     Sedang
  5       UI-1.2      Sidebar admin (desktop)            Desktop admin Tinggi
  6       UI-2.3      Stat cards horizontal + trend      3 halaman     Sedang
  7       UI-3.2      Table rounded-[2rem] (admin)       Admin pages   Rendah
  8       UI-5.1      CSS additions                      Global polish Rendah
  9       UI-3.1      Dashboard quick action shimmer     Admin polish  Rendah
  10      UI-1.3      cmd+K search hint (Navbar+Katalog) UX detail     Sangat rendah

================================================================================
  VERIFICATION CHECKLIST (sebelum commit setiap fase)
================================================================================

  [ ] Visual mobile 375px: sidebar tersembunyi, BottomNav island tampil
  [ ] Visual desktop 1280px+: sidebar tampil, BottomNav tersembunyi
  [ ] Semua nav links & badge counter masih berfungsi
  [ ] Filter tabs di AdminOrders/PesananSaya masih filter dengan benar
  [ ] Contrast ratio status badges ≥ 4.5:1 (WCAG AA)
  [ ] env(safe-area-inset-bottom) di BottomNav tidak hilang
  [ ] PWA standalone mode: layout tidak overlap dengan notch/home indicator
  [ ] Light mode (data-theme=light): tidak ada elemen yang invisible

================================================================================
